/target/
/result
/result-*
.direnv/
.envrc.local
*.swp
*.swo

# Phase 0 measurement outputs — machine-specific; final summary lives in docs/PHASE0.md
/results/

# ds4-dump build artifacts
external/ds4-dump/*.o
external/ds4-dump/ds4-dump-logits
external/ds4-dump/ds4-dump-tokenize
external/ds4-dump/ds4-dump-activations
external/ds4-dump/quants-selftest

# Phase 1 reference outputs — captured logits live outside the repo (huge);
# only docs/PHASE1_REFERENCE.md (with SHA256s) is checked in.
/reference/

# scratch/ HIP probes: source committed, binaries ignored.
/scratch/svm_probe
/scratch/svm_probe_many
/scratch/svm_probe_hostmem
/scratch/plain_probe_many
.rocprofv3/

# Per-session Claude Code local config (not source)
.claude/

# Alternate cargo target dirs (the v41 build uses CARGO_TARGET_DIR=target-v41).
# 2.2 GB of build artifacts — never committed.
/target-*/

# Server/daemon logs and perfetto captures: regenerable, and large.
/logs/
/traces/
scripts/v41_oracle/__pycache__/
